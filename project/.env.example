# ── Supabase (client-safe keys only — NEVER put service_role here) ──
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...your-anon-public-key...here

# ── Demo mode: "true" shows Demo Access buttons, "false" hides them in production ──
VITE_DEMO_MODE=true

# ── Optional: app base URL used in auth email redirects ──
VITE_APP_URL=http://localhost:5173
